buildPlugin(
    platforms: ['linux'],
    jdkVersions: [8],
    findbugs: [run: true, archive: true],
    checkstyle: [archive: true, unstableTotalAll: 0]
    echo "test 4 (ignore)"
    echo "test 4 still (ignore) adding another commit to pull request"
)
